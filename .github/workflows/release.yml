name: Release dingtalk-action

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  packages: write

jobs:
  build:
    name: Build and Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # -------- 🟦 Login to GHCR -------- #
      - name: Login to GHCR
        run: |
          echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      # -------- 🟦 Build Docker Image -------- #
      - name: Build Docker Image
        run: |
          IMAGE=ghcr.io/${{ github.repository }}/robot-dingtalk-go
          TAG=${GITHUB_REF_NAME}
          docker build -t $IMAGE:$TAG .
          docker push $IMAGE:$TAG

      # 可选：自动更新稳定 tag，例如 v1
      - name: Update Major Tag (v1)
        if: startsWith(github.ref_name, 'v1.')
        run: |
          IMAGE=ghcr.io/${{ github.repository }}/robot-dingtalk-go
          docker tag $IMAGE:${GITHUB_REF_NAME} $IMAGE:v1
          docker push $IMAGE:v1

      # -------- 🔓 Force Set Package to Public -------- #
      - name: Force Set GHCR Package to Public
        continue-on-error: true
        run: |
          OWNER=${{ github.repository_owner }}
          PACKAGE_NAME=robot-dingtalk-go
          
          echo "🔄 正在强制设置包为公开..."
          
          # 尝试用户包路径
          curl -L -X PATCH \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/users/${OWNER}/packages/container/${PACKAGE_NAME}" \
            -d '{"visibility":"public"}' && echo "✅ 包已设置为公开 (用户包)" || \
          
          # 如果是组织，尝试组织包路径
          (curl -L -X PATCH \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/orgs/${OWNER}/packages/container/${PACKAGE_NAME}" \
            -d '{"visibility":"public"}' && echo "✅ 包已设置为公开 (组织包)") || \
          
          echo "⚠️ 无法自动设置包可见性，请手动在 GitHub 上设置"
